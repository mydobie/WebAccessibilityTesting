// Helpers methods that can be used across the application

import GraceHopperDoc from '../docs/GraceHopperHandout';

export const docLink = (docHeader: string) =>
  `${GraceHopperDoc.main}${docHeader}`;
